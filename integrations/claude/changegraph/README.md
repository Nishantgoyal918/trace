# ChangeGraph for Claude Code

This plugin shares the same ChangeGraph local service and dashboard as the Codex integration. It includes the ChangeGraph MCP tools, a review skill, and a non-blocking `Stop` hook that launches a graph review when tracked Git changes exist.

Load the directory with Claude Code's `--plugin-dir` development option after starting the local dashboard and service.

